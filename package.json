{
  "name": "quikanva",
  "version": "0.2.1",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "changeset": "changeset",
    "changeset:status": "changeset status",
    "release:version": "changeset version && ./scripts/sync-release-version.sh"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0"
  }
}
